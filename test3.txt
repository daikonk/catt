var sum = func(x, y, channel) {
  rest(2000);
  return channel <- x + y;
};

var channel = noct();
prowl sum(10, 20, channel);
meow("reached");
prowl sum(5, 13, channel);

var x = <-channel;
var y = <-channel;

meow(x + y);
